[build-system]
requires = ["hatchling>=1.25.0"]
build-backend = "hatchling.build"

[project]
name = "ap-python-starter-kit"
version = "0.1.0"
description = "Python starter kit"
readme = "README.md"
requires-python = ">=3.11"
authors = [
  { name = "Template Maintainers" }
]
# ACSys python bindings (pulled via uv git source below)
dependencies = [
  "acsys",
  "acsys[settings]",
]

[project.scripts]
ap-python-starter-kit = "ap_python_starter_kit.main:main"

[project.optional-dependencies]
gui-pyqt = [
  "PyQt6>=6.7"
]

[tool.uv]
dev-dependencies = [
  "pytest>=8.0",
  "ruff>=0.6"
]

[tool.uv.sources]
# Pull from git, matching the reference container example.
acsys = { git = "https://github.com/fermi-ad/acsys-python", rev = "v0.12.9" }

[tool.pytest.ini_options]
pythonpath = ["src"]
addopts = "-q"

[tool.ruff]
line-length = 100
target-version = "py311"

[tool.ruff.lint]
select = ["E", "F", "I", "UP"]
